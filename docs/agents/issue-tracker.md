# Issue tracker: GitHub

Issues and PRDs for this repository live in GitHub Issues. Use the authenticated
`gh` CLI and infer the repository from its Git remote.

PRs as a request surface: no.

When a skill says "publish to the issue tracker," create a GitHub issue.
Use `gh issue` commands to create, read, comment, label, and close issues.
