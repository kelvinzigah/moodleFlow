# moodleFlow
moodle Automation tool

install requirements on linux:

cd "/home/kelvin/path/to/project/moodleFlow" && venv/bin/pip install -r requirements.txt

install requirements on windows:

cd "c:/Users/KZIGAH/Desktop/Coding Projects/moodleFlow" && venv/Scripts/pip install -r requirements.txt

use use python -m pip install to install packages (linux)